<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="action_cancel_windows" model="ir.actions.act_window">
            <field name="name">Cancel Order Kamar Kostan</field>
            <field name="res_model">idita.order_kostan</field>
            <field name="view_mode">tree,form</field>
            <field name="domain">[('state','=','cancel')]</field>
            <field name="context">{'default_state':'cancel'}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Silakan Order Kamar Kostan
                </p>
            </field>
        </record>
    </data>

    <menuitem id="action_cancel_menu_act_id" name="Cancel Order Kamar Kostan" action="action_cancel_windows"
              parent="service_kostan_menu" sequence="12"/>

</odoo>